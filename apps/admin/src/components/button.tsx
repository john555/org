import { styled } from "@styles/stitches.config";

export const Button = styled('button', {
    backgroundColor: '$primary',
    p: '$2 $5',
    br: '$round',
    borderWidth: '$default',
    font: 'inherit'
})